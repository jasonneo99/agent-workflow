import assert from "node:assert/strict"; import test from "node:test";
import { buildLearningApplicationPlan, buildLearningApprovalQueue, decideLearningApprovals, type LearningProposalSet } from "./index.js";
const proposals: LearningProposalSet = { kind: "agentflow_learning_proposals", projectRootUri: "/portable/project", generatedAt: "2026-01-01T00:00:00.000Z", sourceReportGeneratedAt: "2026-01-01T00:00:00.000Z", sourceRunsAnalyzed: 2, summary: [], proposals: [{ id: "learn-low", priority: "low", kind: "route_feedback", riskLevel: "low", title: "Route", target: "x", rationale: "evidence", evidence: [], recommendation: "refresh", approvalRequired: false }, { id: "learn-high", priority: "high", kind: "eval_gap", riskLevel: "high", title: "Eval", target: "x", rationale: "gap", evidence: [], recommendation: "evaluate", approvalRequired: true }] };
test("approval governance auto-approves only eligible risk and preserves explicit gates", () => { const queue = buildLearningApprovalQueue(proposals, "all", undefined, "medium"); assert.equal(queue.items[0].status, "approved"); assert.equal(queue.items[1].status, "pending"); const decided = decideLearningApprovals({ queue, ids: ["learn-high"], status: "approved", reviewer: "owner" }); const plan = buildLearningApplicationPlan(decided.queue); assert.equal(plan.actions.length, 2); assert.equal(plan.actions.find((action) => action.proposalId === "learn-high")?.dangerGate, "approval_required"); });
